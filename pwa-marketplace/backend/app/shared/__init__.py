# This file can be empty, but must exist to mark the directory as a package, and
# allow relative imports to work in the shared module. Eg. `from . import config`
# or `from .config import Config` in other modules within app.shared
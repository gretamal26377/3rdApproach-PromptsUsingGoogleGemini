# Change this to your API base URL when it's defined
# Not used by the moment. Env vars will be set up in docker-compose files 
# VITE_API_BASE_URL=http://your-api-url.com

# This doesn't work because .env file can't reference/substitute other variables
# VITE_API_BASE_URL=REACT_APP_API_BASE_URL
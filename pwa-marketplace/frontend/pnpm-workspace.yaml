# Recommended definition if pnpm-workspace.yaml is inside the 'frontend' folder
packages:
  - 'admin'      # Looks in frontend/admin
  - 'customer'   # Looks in frontend/customer
  - 'shared-lib' # Looks in frontend/shared-lib
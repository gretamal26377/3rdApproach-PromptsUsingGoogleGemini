# Admin frontend Vite env vars

# Avoid hardcoding keys here, because of Source Control (committing to public repos), and if you do,
# don't forget to add this file to .gitignore

# Not used by the moment. Env vars will be set up in docker-compose files
# VITE_API_BASE_URL=http://localhost:5001/api
# VITE_SEARCH_API_BASE_URL=http://localhost:5002/api-search
